# Configuration Guide

This page is reserved for the public configuration guide of `ManipPilot`.

As the first runnable baseline becomes available, this page is expected to document:

- required configuration files
- supported environment variables
- configurable task inputs
- runtime options that users may need to modify
- setup notes for supported environments

At the current stage, the public configuration surface is not published yet.
